In this latest commit there are two changes. The first change is moving target pitch to params instead of a hardcoded 5 degrees. These changes built successfully and the drone flies without GPS and compass into a hardcoded heading.  Then I didn't commit and implemented listening to RC to set the param for heading during flight. It builds successfully, but the drone is bricked. It doesn't work. So the latest working asset we have is POC 0.0.4, and the latest (but it doesn't work) is POC 0.1.0. Need to try incrementally revert changes, commit changes needed for pitch param, and revert incrementally other changes; in this way we can detect what's wrong. Also, I can just stash all changes, go to the previous commit, and try to build to verify if it's a code problem and not something like submodules or something else.